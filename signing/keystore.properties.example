# Relative paths resolve from the repository root. For a production key, use an
# absolute path outside every source checkout instead.
storeFile=/absolute/private/path/comicviewer-production.jks
storePassword=replace-me
keyAlias=comicviewer
keyPassword=replace-me
